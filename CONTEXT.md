# CONTEXT.md

clj-harness 是一个 Clojure 写的 agent 内核：跑一轮的接口是 AG-UI（`POST /api/agent`），其余 `/api/*` 是
管理边；**会话归服务端**（内存是权威、记录是恢复源），浏览器是只读副本。前端是 TypeScript。

本文是**领域术语表**：这里定义的词，issue、spec、代码注释、测试名一律照用；同义词是要避免的，不是要
宽容的。它不是教程也不是需求——需求在 `.scratch/<slug>/spec.md`，架构决策在 `docs/adr/`（还没有条目
就是还没有）。

## 会话与项目

**会话**（thread-id）—— 一段**服务端持有**的对话：进程内存里的那份是权威，记录
（`~/.clj-harness/projects/<workspace>/<thread-id>.jsonl`）是它的**恢复源**、可以落后，
永远是它的**有序前缀**（一行一个 JSON、一个写者按发生顺序 append）。它也是**归属的单位**：
工具表、锚点归属、待决审批都按它分。一轮 run 的输入是**动作**（服务端从会话里取历史），
浏览器手里只有它的一段**窗口**。workspace 由项目身份（canonical 路径）算出、未绑定的落在
`projects/.unbound/`——**一个会话一份文件**，所以换绑会把文件一起搬
（见 [home-and-storage](docs/architecture/home-and-storage.md#一个会话一份文件)）。
*别叫成* 对话（conversation 是消息序列，不是这个单位）、客户端状态（会话不是浏览器持有的
东西）、缓存（记录不是缓存，它是恢复源）。

**项目绑定** —— 把一个会话接到一个目录上（`/api/project`）。绑定之后相对路径以那里为根，
`.harness/harness.edn` 也只有绑定之后才有意义。

**窗口（window）** —— 浏览器手里那一段会话：`{entries, baseSeq, hasMore, cursor, generation, state,
revision}`。条目按发生顺序，每条带一个**序号**（`seq` = 它在记录里的偏移）；`baseSeq` 是这段窗口
最前面那批的号，`hasMore` 说它前面还有没有东西，`cursor` 是它手里最新的号，`generation` 是它属于
哪一个窗口（见下），`state` 是会话此刻在跑还是停（`running` / `parked` / `settled` / `unfinished`），
`revision` 是**这份窗口变过几次**（副本自己的计数，**不是消息序号**——把它当 seq 用是这套设计里最
容易犯的那个错）。
*别叫成* 缓存、「已加载的消息」（窗口不是截断的历史，它是**可续的一段**：前面的按页补，新的由 feed 推）。

**尾页（tail page）** —— 打开一场会话先拿到的那一页，是**最新**的一页（条数是一个写下来的数字，
`page-size` = 50，切在**批**的边界上）。刷新也是拉尾页，所以落点天然是最新。
*别叫成* 全量、第一页（它是最新那一页，不是最开始那一页）。

**补页（page in front）** —— 往**更早**的方向按需拉一页（`GET /api/threads/<stem>/page?beforeSeq=N`），
界面上是「显示更早」那颗按钮，一次一页、一次只有一页在飞。补页是 `prepend`（往读者眼前插），
所以它必须**锚定**：读者正在看的那条消息的位置、输入框里打了一半的字都不许动。
*别叫成* 分页（那是列出很多东西的说法）、自动加载（不会自动往更早的方向长，只有这颗按钮）。

**feed** —— 一场会话的那条**连接**（`GET /api/threads/<stem>/feed`，SSE）：首帧是尾页（或从读者
游标之后的增量），此后每落盘一批推一帧，窗口作废时发一帧 `end` 再关。**游标随连接走**（`since=N`
与 `generation` 是连接自己的参数）：服务端除了活着的连接**不记谁在订阅**。
*别叫成* 订阅（服务端不记订阅这件事）、轮询（恰恰相反：没事发生就不发问）、消息队列。

**generation** —— 一个窗口的身份，就是**票 04 那张认领行的 token**。会话被放掉（空闲 30 秒）、
认领易主（另一个进程接手）、地址被替换 ⇒ 这个 generation 作废：feed 的 `end` 帧或一次 409 说出来，
副本**重开**（重新拉尾页），而不是停在一段已经作废的窗口上。
*别叫成* 版本号（它不是内容的版本，是**谁在服务这场会话**的凭据）、session id（那是 thread-id）。

**任务** —— 一段**没有家**的会话：库里 `project-id` 与 `last-project-path` 一起为 NULL。
**它是被发送造出来的，不是被点击造出来的**：界面上「新建任务」只铸一枚 id、在页面里打开一场空会话
（不写库、列表上什么都不出现），到了第一句真正发出去之前，页面拿这枚 id 走一次 `POST /api/sessions`
把它登记成这个家的一条会话（find-or-create，已经有就原样不动）；服务端**不再从陌生 id 静默建会话**
——run 那条边对陌生 id 是 404，所以登记必须发生在那次 run 请求之前。界面上是侧边栏顶部那块平铺、
不分组、不属于任何项目的列表，归档落在底部那一块「已归档」里。**当一个会话被移除的项目放走时
它不是任务**——它记得那个目录（`last-project-path`），所以它等的是那个目录回来，而不是变成
一段无主的对话。
*别叫成* 无项目的会话（「未绑定」是两个原因的合称，任务是其中一个）、未保存的会话、草稿；
**也别和「任务清单」混**——那是本会话的待办，由 `todo_write` 写，和这条没有关系。
*别叫成* 工作区（workspace 指进程的当前目录，不一定是绑定的那个）。

**会话标题** —— 一场会话**第一句有文字的用户消息**，画在对话列顶栏、侧边栏那一行、也进浏览器 tab
（`<标题> · clj-harness`）。**它落盘**：第一次收到消息的那次 run 把它写进库里的 `sessions.title`
（只写一次、永不改写），`GET /api/projects` 每一行都带。（这一条 2026-09-21 由主人推翻过——
先前写的是「派生量、不落盘」，代价与反转的理由在 `.scratch/session-titles-in-the-store/spec.md`
与 `harness.infra.db/sessions-remember-their-title` 里。）
**老会话没有这一列的值，也不回填**，侧边栏退回显示 thread-id；顶栏没说话时用词表里那句
`session.untitled`（`New session` / `新会话`）。
*别叫成* 名字 / 命名（**本产品没有重命名这个动词**，标题是「第一句」而不是谁起的名字）。

**上次发送时间**（`sessions.last_sent_at`）—— **人按下发送**的那一刻，不是「日志最后被追加」的那一刻：
每次 run 的 input 到达时重写，与标题同一句 `UPDATE`（`cap.project/remember-send!`），续跑也算一次发送。
侧边栏每一行的右端画它，**是相对时间**（`刚刚 / N 分钟 / N 小时 / N 天`，超过 7 天给日期
`9/14`；完整绝对时间与 thread-id 一起在 hover 的 tooltip 里）。列表按它降序、没值的沉底；
老行由 `sessions-remember-their-last-send` 用日志 mtime 回填过一次（与标题相反，**这一列回填**）。
*别叫成* mtime / 最后活动时间（磁盘那个数退出了界面：侧栏一行现在只有
`[spinner 槽][标题][相对时间]`，**没有文件大小**，也**只有库能答的字段**加一个进程内的「在跑没有」）。

**配置两级** —— 配置家 `~/.clj-harness/harness.edn`（用户级）与绑定项目里的 `.harness/harness.edn`
（项目级）。顶层浅合并：项目提到某个键就整个换掉用户的。**唯一的例外是 `:editing`，它逐键合并**——
所以项目级只写 `{:grep false}` 不会把用户级的 `:mode` 一起抹掉。

**提供方**（provider）—— **一家厂商**：一个 endpoint、一个协议，model 挂在它下面。它不是 model，
也不是某个会话的设置：会话用三个旋钮**选择**它（见下）。它住在 `config.edn` 的 `:providers` 一节里，
界面上的「Models」页就是它的增删改。
*别叫成* 供应商 / vendor（同一个东西的洋名，代码里一律 `provider`）、模型（model 是厂商**下面**的一个 id）。

**凭据名** —— 一个 provider 的密钥在 `.env` 里的名字：**由它的 id 派生**（id 全大写、非字母数字换成
下划线、缀 `_API_KEY`）。`:acme-gateway` → `ACME_GATEWAY_API_KEY`。查找顺序是**本 provider 的名字
优先，全局 `HARNESS_API_KEY` 兜底**，所以只写过全局那把的家照旧能用。派生是**全函数但不单射**
（`:a-b` 与 `:a_b` 撞名），挡住它的是表单的 id 判据。
*别叫成* 密钥名 / 凭据 ID（名字是派生出来的，不是另一个标识）。

**默认档** —— `config.edn` 的 `:default` 一节：三个旋钮的**起点**，也就是**新会话**从哪儿开始。
它坐在三档的最下面（默认档 → 会话档 → 本次请求）；界面上改它的是「General」页。
*别叫成* 全局设置 / 默认配置（「档」在这份表里是有层级的词，见「三个旋钮」那三档）。

**三个旋钮** —— `:provider` / `:model` / `:reasoning-effort`。**只有这三个**是「选择」，其余关于
endpoint 与模态的事实都是目录**回答**的（写进某一档就是指名失败）。三档逐旋钮合并，
**换 provider 而不指定 model 就落在新厂商的默认 model 上**。
*别叫成* 配置项 / 参数（旋钮是**可被某一档选**的那三个，别的都不是）。

**思考模式**（thinking mode）—— 厂商在**请求带 `reasoning_effort`** 时进入的模式：它会把推理过程单独
流回来（`reasoning_content`），并**要求历史里每条 assistant 消息都把那个字段送回去**，缺了就是 HTTP 400。
「这一轮没有推理」厂商也是**带一个空值**说的，所以空串要原样留着；历史里真的没有这个字段时，
我们在请求侧补一个空串（只补空串，不编内容）。见 [providers](docs/architecture/providers.md)。
*别叫成* 思考档（那是 `:reasoning-effort` 这个**旋钮**本身，是选 low/medium/high 的地方）。

## 轮与模型调用

**轮** —— 一条用户消息，以及它引起的**全部**输出（可能含多次**模型调用**）。
它**不等于**一次 run：悬置恢复会让同一个 `runId` 出现第二个 `input`，那仍是同一轮。
*别叫成* turn / 回合 / 迭代。

**模型调用** —— 一次 provider 流式生成（`harness.kernel.llm/stream!` 的一趟），加上它为它要的那批工具调用
（那批工具调用是**并发**跑的）。它在记录里是一对 `model/start` / `model/end` 行。
条子上那一格写英文 `step`，但**中文正文一律写「模型调用」**——本表不新增「步」这个量词。
*别叫成* step / round / 回合 / LLM call。

## 编辑

**编辑模式** —— 本会话被服务哪一套编辑实现，取值 `:hashline`（默认）或 `:str-replace`，写在
`harness.edn` 的 `:editing {:mode …}`。它不是开关 API 而是配置；每次调用现读，改它不需要重启，
而且**按会话解析**（两个项目各选一种，是日常情形）。自省入口是
`(harness.cap.editing/editing-mode harness.kernel.tools/*thread-id*)`。
*别叫成* 编辑后端、edit mode（中文一律写"编辑模式"）。

**工具表** —— 一个会话此刻被服务的工具集合（名字 + 描述 + 参数）。编辑模式是它的又一个输入：
`:hashline` 下 `edit` **不在**表里，`:str-replace` 下 `replace` / `insert` / `grep` /
`undo_last_replace` **不在**表里。被拒的名字仍然存在——拒绝信息说清本会话编辑用什么、以及哪个配置键
换回去。

**锚点** —— 一行的 4 字符名字（例如 `a3f9`）。它由**分配**得来（从一张 vendored 的表里取下一个没用过
的），不是从内容算出来的；同内容的两个行**绝不**共用一个锚点。它**只属于铸造它的那个会话**，
另一个会话读同一个文件拿到的是另一套。一行不动，它的锚点一直是它。`read` 输出成 `锚点│内容`。
*别叫成* hash（哈希从内容算出来，锚点恰恰不是）、行号（行号随行移动漂移，锚点不随）、id。

**已展示行** —— 本会话**真的把这一行连同锚点印出来过**。它是一个集合，与"归谁所有"是两件事：拥有是
铸造时的记账，已展示是"模型见过它"。`read` 与 `grep` 印出来的算，两者被拒时回带的那几行也算；
同一个文件里没印出来的行不算。**只有已展示的行能被编辑**——锚点对得上但从没展示过，拒绝。
*别叫成* 已读、可见（visible 太宽，一行没印出来也是"可见"的）。

**漂移** —— 这个会话读到某一版之后，文件在磁盘上被改过了。判据是逐行校验和不一致，不是时间戳。
漂移时不静默重定位：编辑被拒，拒绝里带**当前**的锚点，所以重试不必先重读。
*别叫成* 冲突（conflict 是两笔写撞上；漂移是别人先写了）。

**拒绝即交付**（refusal as delivery）—— 编辑被拒时回的不只是一句错：它带上它当时在说的那几行、连同
它们**现在**的锚点，并把这些行登记为已展示。于是"重试"是一次新的编辑调用，不是一次重读。漂移、
未展示、需要救援这三种拒绝都是这个形状——这是本特性的脊柱，不是三种各自发明的东西。

**批**（batched edit）—— 同一**模型调用**里对**同一个文件**的多次编辑。一次模型调用的多个工具调用是并发跑的，
两笔各自针对同一基态的编辑各自都成立、合起来丢数据；所以它们被当成**一次提交**：全部针对消息开始前的
状态校验、区间两两不相交、最后一次调用给出合并后的 diff、**一次写、一次撤销**；不成立就整批拒绝。
*别叫成* 事务（那是库层面的词；批是模型一次调用的编辑单位）。

**撤销记录** —— 每个文件最近一次编辑前的正文、编码、锚点，以及改完后的正文。只保留最近一次，
一次 `write` 就清空；`undo_last_replace` 读它。锚点与文件**一起**退回：只还原文本会让库里那套锚点
描述一个已经不存在的状态。
*别叫成* 备份、历史（都不保留多次）。

**工具名的写法**：`read` / `write` / `edit` / `replace` / `insert` / `undo_last_replace` /
`grep` / `bash` / `job` / `job_kill` / `job_output` / `eval` / `glob` / `todo_write` /
`web_fetch` / `web_search` / `ask` —— 就是这些名字，
不给它们起别名，也不把 `edit` 叫成"旧编辑"、把 `replace` 叫成"锚点编辑"。
一律小写、多词用下划线（`undo_last_replace` 就是那个先例）：不写 `camelCase`、不写 `PascalCase`
——别人家的清单里写 `WebFetch`、`TodoWrite`，那是别人家的写法。

**回执（receipt）** —— 一次**写**类调用的答案：只说**事实**（写了多少字、清单存了几项、各自什么状态），
**不回放模型自己刚送进去的那份东西**。理由是同一份 token 不付两次：内容就在这次调用的参数里、历史里躺着，
念回来既不添信息也不改状态；而「交回头二十行带锚点的行」那种**有限回放**更糟——它会被读成「整份都能编辑」
（`.scratch/write-no-content/spec.md` 有那一课的全文）。`write` 与 `todo_write` 的答案按这条走；
`read` 那一类的答案不是回执——那些内容模型手里**没有**，交出来才是答案。
回执**可以**带一句**指路**：`write` 说「锚点已释放，去 `read`」，`job` 与 `job_kill` 的回执说「用
`job_output` 读它」（通知的第三行是同一句）——指的是模型**还没走过**的那一步，不是把已有的东西念一遍。
**但通知不是回执**：那条注入还会把作业跑的那条**命令原文**带回来（`<command>…</command>`），
因为通知可能隔了好几轮才到、而 `j1` 光靠 id 认不出是哪个作业——**认不出来就等于没提醒**。
（`.scratch/receipts-not-echoes/spec.md` 决策 4。）
*别叫成* 摘要（summary 是压过的东西，回执不压）、确认（ack 只讲成败，回执讲事实）、回声（echo 正是要避免的）。

## 跑命令

**时限（timeout）** —— 一次 `bash` 调用**等人多久**的上限，毫秒，默认 120000。到点把命令**连它起的
子孙**一起停掉，命令在此之前打出来的东西原样回到模型手里，末尾多一行说它停在哪一刻。
它不是审批那条「不做超时」：那一条讲的是**人一直不响应就一直待决**（有意的无限等待），
时限讲的是**命令**的边界，两件事不要读成一件。
*别叫成* deadline、执行预算、超时配置。

**后台作业（job）** —— 一条**没人等**的命令：`job` 起它（**三个动词各做一件事：起、读、停**，
不是一个动词带一个模式开关）、`job_output` 读它（可以挂到终态）、`job_kill` 停它。
作业**没有时限**（跑到自己结束，或跑到被停；`timeout` 与 `stdin` 都不是 `job` 的参数——
**字段删掉，不是拒绝**：这份 schema 就是它的全部语言），
它打出来的**每一行**都落进自己那份记录
（`<配置家>/jobs/<会话>/句柄-进程戳.log`，一份作业一份），末行说它怎么了：`[exit N]` / `[stopped]`，
没有那一行就是还在跑 —— **状态就是这一行，本仓不另立一套枚举**。模型读它的是 `job_output`（状态 + 一段窗口 +
可以等）；记录本身是一份文件，落在围栏的自由路径上（`bash` / `read` / `grep` 读得了它）。**两份回执不报路径**
（`job` 与 `job_kill` 报的是 `job_output`），**而读的那一个报**：答案装不下整份记录时，`job_output` 把那份记录
的路径说出来（`.scratch/job-receipt-no-path`）。
**作业在进程里，记录不在**：按会话分家的注册表随 JVM 退出消失（`job_output` / `job_kill` 从那一刻起答
「未知作业」），而**文件留在配置家**——名字里的那截进程戳说的就是「哪一次运行写的」，所以下一次运行不会
把手伸到上一次的记录上。不进库、不是会话历史；也不随 run 结束而死 —— 随 run 死就等于后台执行没用。
停过的作业**条目仍在**（答得出终态），第二个 `job_kill` 拿到的是同一个答案。
*别叫成* 任务（那是任务清单）、daemon、服务（MCP 服务器是另一件事：它是会话级资源）。

**告知（told）** —— 一个作业的结局**已经交到模型手里过**这件事：`job_output` 读到了终态、`job_kill` 的
答案带着结局、或者那条通知被注入进历史。它记在**作业注册表**里（进程内、按会话、与作业同寿命），因为
通知没有技能正文那样的锚（客户端从不持有它，历史里没有标签可查）。**一个作业只说一次**——已经读过的
结局不是新闻；模型自己 `bash tail` 那份记录不算告知（我们看不见），于是会多收到一次通知，这是认下的代价。
*别叫成* 已读（那是编辑那边的词）、通知（通知指那条**注入本身**，告知指那个**事实**）。

**注入（injection）** —— 「每次模型调用前，把会话该有的东西摆到历史里」这件事，以及被摆进去的那些块：
**开场块**（指令文件、技能清单）、**技能正文**（`skill` 工具或人的 `/name` 触发）、**作业结束的通知**
（`<job-ended id="…">[exit N]</job-ended>` + `<command>…</command>` + 一行「用 `job_output` 读它」
——**两样事实加一句读法，与记录多大无关**）三种用户。它们都是**服务端现算**的：客户端从不持有，只有这一轮发给
模型的那一份（所以也照旧写进 jsonl 的 `message` 行）。位置是**提问之后**（system → 提问 → 出生那条 context → 开场块：指令文件 →
技能清单 → 技能正文）：材料排在**它要回答的那句话后面**，`.scratch/context-frames` 决定 7 起就是这么读的。开场与其余两样的区别
不在位置而在**次数**——开场在会话出生时写进对话**一次**，此后每轮它只是历史（`.scratch/session-opening`）。
**每一份注入在屏幕上都有一张卡**（`CUSTOM` 帧 → `data` part → 会话栏里可折叠的一行），
**而卡就是这条注入留在对话里的样子**：`data` part 给屏幕，`sessions/model-view` 把卡里的字节还原成模型读过的那条 user 消息——
所以注入只进一次，下一轮 `cap.project/before-llm` 发现正文已在，既不重算也不再画一张卡。
轨迹那一栏照旧读 `message` 行。**开场那一次的卡跟着写它的那一轮出去**，id 就是条目自己的（`session-opening-<i>`）——
自己开出这一页的那个客户端既没有窗口也不跟 feed，除了这一轮没有别的线能把它送到；派生注入的帧 id 才是 `<run>-ctx<i>`。
两处组装：出生那一轮在 `edge/http.clj`（开场条目与它的卡一起进去），此后每轮在 `cap.project/before-llm`。
*别叫成* 上下文（太宽：上下文还包括客户端手里那段窗口）、系统消息（那是 `system` 角色的那条，另一件事）、
推送（注入等着模型开口，不唤醒任何东西）。

**作业的读法（job_output）** —— 后台那一族里**读**的那个动词：头一行是状态（记录的末行，或 `[running]`），
正文是命令说过的**一段窗口**（不给 `offset` 就是尾部），结尾一行说这段是记录的第几行到第几行。
`offset` / `limit` 是**调用者**给的位置（一行一号，与 `grep -n` 数的同一批行号），不是本仓替它记的游标；
`wait: true` 挂到记录写完末行那一刻，`timeout` 到了答 `[running]`（**不是错误**）。它是**唯一**会阻塞的
后台动作 —— 作业没有推送通道，所以「等」只能自己开口。
*别叫成* 游标（那是上一版删掉的东西：位置在调用者手里，本仓不替谁记「读到哪了」）、通知、轮询。

**答案上界（answer budget）** —— 一次 `bash` 调用能带回多少**字节**的命令输出（一个 `def`，两个工具描述
里插值）。按字节不按行（一行 100KB 的 JSON 撑爆按行方案），留**尾部**不留头部（要读的在尾巴），
stdout 与 stderr **各算各的**。超过就整份落成一份记录，答案里写「省略 N 字节」与那个路径。
*别叫成* 截断（截断只说「少了东西」，这里说的是少了多少、去哪拿）、日志轮转。

**`stdin` 与 `workdir`** —— 前台 `bash` 的**进路**：喂给命令的标准输入（写完随即关掉，所以读它的命令
看到 EOF）、以及命令在哪个目录里跑（相对路径按本会话的项目绑定解析，与 `read` 同一处；不给就是项目目录）。
`workdir` **不挂审批**：`bash` 本来就没有围栏，拦它的目录是装样子 —— 判据只有一条，它得是个目录。
*别叫成* cwd（那是进程的事实，这里是这次调用指名的地方）、管道（`stdin` 不是 `|`）。

**`shell`（选壳）** —— 一次 `bash` 调用**指名它跑在哪只壳里**：`git-bash` / `bash` / `cmd` / `pwsh` /
`powershell`，不给就是**这台机器自己那只**（Windows 上是 Git Bash）。它换的是**解释 `command` 的那只
壳**，不是「同一行字换个人执行」—— `%CD%` 只对 cmd 有意义，`$env:TEMP` 只对两个 PowerShell 有意义。
指名一只本机没有的壳、或一个不是壳名的词，**按名字拒绝而不回退**：在没人要的壳里跑，调用方写的语法
会被一个不懂它的人解释，答案看起来就像命令自己的 bug。前台与后台同一个名字、同一条规矩。
*别叫成* shape（那个词已经指 `:program` 与 `:shell` 那根轴）、终端类型、方言。

**句柄（job id）** —— 一个作业在本会话里的名字，形如 `j1`。**只在会话里有意义**，而且**不回退**：
用掉的名字不再出现，所以一句旧答案里的 `j1` 不会指向另一条命令。
*别叫成* pid / 进程号（那是指向操作系统进程的东西，句柄是模型手里的一句话）、编号。

**作业记录（job record）** —— 一条命令自己那份输出文件：`<配置家>/jobs/<会话>/<句柄>-<进程戳>.log`
（后台作业从一开始就写，前台 `bash` 的答案超过预算时也写一份），命令每打一行就追加一行、flush 一行；
末行是本仓补的那一句（`[exit N]` 或 `[stopped]`，**没有那一行就是还在跑**）。
它不是**会话历史**（本文件里那个「记录」：jsonl 的消息流水）——不进 jsonl、不进库、不加审计行；**它活过
写它的那个进程**（这正是留它的理由：昨天那次的现场，今天还读得到）。读它的是模型手里已经有的读法：
`bash`（shell 的 `tail` / `grep` / `cat`）、`read`、以及工具 `grep`；整棵树按字节封顶，超了从**最旧**的一份开始删。
*别叫成* 日志文件（那是这台机器的运行日志）、输出缓冲（buffer）、尾巴（`tail`，是本特征删掉的旧形状）。

## 技能

**技能层** —— 技能根按来源分的一档。今天两档：**系统级**（`<OS 家目录>/.agents/skills`）与
**项目级**（`<项目>/.agents/skills`）。同名时**前面的根赢**，所以默认顺序下**系统级赢项目级**——
项目里放一份与机器上同名的技能，在清单与列表里都不出现。`harness.edn` 的 `:skills {:roots […]}`
写的根**没有层**：配置表里没有「系统 / 项目」这回事，从列表位置猜一个层名就是把猜测写成事实，
所以那种根只报出路径。
*别叫成* 用户级、全局、global、scope、来源。

**技能列表** —— 输入框里打 `/` 弹出的那份菜单。它列的是这场会话的技能：**能加载的**都在，
包括 `disable-model-invocation: true` 的（文件说的是模型不该自己决定去用，人打 `/name` 就是人在决定）；
**坏掉的也在**——带原因、但选不动。每行标明它的**技能层**，平铺成一张表，不分节。
*别叫成* picker、命令面板、补全、下拉。

## 状态住在哪

**库装状态、文件装记录。** `~/.clj-harness/harness.infra.db`（sqlite）装"现在是什么"：项目、会话、
**任务清单**（`todos` 一行一个会话），以及锚点
（`hashline_snapshots` / `hashline_ownership` / `hashline_sessions` / `hashline_undo` 四张表）。
日志 jsonl 装"发生过什么"：每一帧、每一次调用与结果。**库不镜像日志**——新表进库，必须有人先写下它
是状态还是记录（守着这条边界的是 `harness.infra.db-test` 的元断言）。

**会话统计** —— 一条会话的**记录**折出来的几个数：轮数、模型调用数、输出速度、用量、缓存命中。
读法住在 `harness.edge.stats`，从管理边由 `GET /api/threads/<stem>/stats` 吐出去，
显示在 composer 下面那条状态条上。它读的是**记录**不是状态，所以**不进库**；客户端不算它
（它手里只有一段窗口，数得出一屏有几条消息，数不出这条会话发生过什么）。
*别叫成* metrics / telemetry / 指标 / 遥测。

**缓存命中** —— 厂商报回来的「这次请求的 prompt 里有多少是它自己的前缀缓存」，记在那一次**模型调用**的
`usage` 里。判据是**厂商报过**才算数：没报就是没报，不填 0。
*别叫成* cache hit rate / 命中率。

**上下文占用** —— 一次**模型调用**把模型的**上下文窗口**填到了多少：分子是厂商在那一次调用的 `usage`
里报的 `prompt_tokens`，分母是**那一次调用自己**的 `model/start` 行上记的 `:context-window`
（目录为那个 model 声明的；日志早于这个键时才回退读 provider 时间线）。读法住在 `harness.edge.context`，
作为 `GET /api/threads/<stem>/stats` 载荷里的 `context` 一节吐出去，画在 composer 里 model 左边那颗圈上。
它读的是**记录**不是状态，所以**不进库**。

**那三样**（系统提示词 / 工具定义 / 对话消息）是**摊出来的估算**：厂商只报总数，不报这个三分，
所以三个篮子按记录里各部分的字符数去摊那个总数——它们因此**恰好加起来**等于分子。**估的是摊法，不是数**：
分子与分母都是厂商/目录说的实数。
*别叫成* context bar（那是 composer 上方那条**上下文条**：这场会话跑在哪个目录、哪个分支）、
token 计数（本仓不数 token、也没有 tokenizer）、上下文窗口（那是目录里那个数字本身）。

**压力**（pressure）—— **下一次请求**会把模型的**上下文窗口**填到多少：一个数，以及**这个数是从哪儿
来的**（`:baseline`：`usage` = 拿厂商上次报的真数当锚点、只估增量；`estimated` = 没有可复用锚点，整段
按字符估）。锚点只在**信封没变**时采用（工具表 / 路由 / 系统消息三者都一致），而且只在厂商报的量不低于
估算器自己的读数时采用——估算器对中文与 JSON schema 系统性偏低，正是因为偏低才敢拿它当锚。读法住在
`harness.edge.pressure`，作为 `GET /api/threads/<stem>/stats` 载荷里的 `pressure` 一节吐出去；每次 run
还会在**第一次模型调用之前**往**记录**里写一条 `context/pressure` 行。
*别叫成* 上下文占用（那是**已经发出去**的那一次调用，厂商量过，是上一条）、token 计数（本仓不数
token，只有一个 4 字符 ≈ 1 token 的估算器，而且只作用在**增量**上）。

**阈值比例 / 保留比例** —— 一对比例：`thresholdRatio`（默认 **0.7**）是压力越过它就**开始压缩**的那条
线；`retainRatio`（默认 **0.16**）是压缩**逐字保留**的最近那段窗口。保留必须**严格小于**阈值，否则压缩
会把它被要求缩小的东西全留下。两个数都住在 `harness.edge.pressure`（默认值），由 `harness.edn` 的
`:compaction` 块按 key 覆盖；自动触发与手动 `/compact` 都照它动手。

**压缩**（compaction）—— 把一段历史从**模型视图**里去掉、换成一条**摘要**，而**记录**一行不删、**会话投影**
照旧（模型读摘要，人读原文）。记录里它是一条 `context/compacted` 事实行：`:shadowed` 是权威的、**按 surface
顺序**列出的被遮蔽节点的记录 seq，`:summary` 是那段摘要文本；`replay/compacted-messages` 把它折进模型视图，
摘要落在**被遮蔽段原来的位置**上（旧内容在前，逐字保留的尾部在后）。一个被遮蔽的节点既是一条**条目**，也可以
是**更早一次压缩的摘要**（用那次事实自己的 seq 命名）——所以 `start` 可以大于 `end`，一切按**位置**走，绝不
拿两个数比大小。
*别叫成* 摘要（摘要是一次压缩的产物，压缩是那件事）、剪枝（剪枝动的是单条工具结果里的文本，不是一个段）。
**超窗恢复**（overflow recovery）—— **厂商已经以「上下文超限」拒绝**之后的那次补救：**不读压力、不读窗口**
（既然已经被拒，就不需要任何容量元数据来论证该压缩了），绕开保留预算、把**最近一个不可分单元**（最近一条
`user` 消息及其之后）之前的历史一次压掉；只有**模型视图真的变短了**才**在同一个轮里**重试，否则把厂商的原话
**原样**交出。判据是厂商自己的句子（`harness.kernel.llm/context-overflow?`，不是所有 400 都算）；
`harness.edn` 的 `:compaction :overflow-retries`（默认 **1**，`0` 关闭）封顶重试次数。
*别叫成* 自动压缩（自动压缩是**预测**窗口要满而先动手；超窗恢复是**厂商已经拒绝**之后的补救）。
**剪枝**（pruning）—— 压缩之前那一步**免费**的缩减：把一条**超大工具结果**从中间砍掉、保留头尾，
模型仍看得到输出怎么开始、怎么结束。**不调模型、不花钱**——这就是它与压缩的全部区别（压缩为一条
摘要付一次调用）。消息还是那条消息：只动 `role: "tool"` 消息的**文本主体**，`tool_call_id` 与
「这次调用被回答了」的配对一点不动；记录里原文一行不删，`context/pruned` 收据记下被遮蔽的事件、共享
的调用 id、以及替换前后的**码点**数，`harness.edge.replay/prune-messages` 让模型读砍过的版本。它
**不完备**：只动工具结果里的文本，压不掉人贴的一长段、也压不掉信封。阈值与头尾长度是
`harness.edge.prune` 里的常量（6000 / 2000 码点）。
*别叫成* 压缩（压缩换掉一整段历史、当场付一次摘要调用；剪枝只动一条工具结果、不调模型）。
**取货单 / spill** —— 压缩、剪枝之外的**第三档**：一条**刚产生**的工具结果太大，就在**产生的那一刻**把它
整个挪到别处，模型只读到一张**取货单**：一段**预览**、一个**不透明定位符**、一句**取回指引**
（`harness.cap.spill`）。它解决的是「**单次输出**就巨大」（一次抓回一个网页、一次匹配几万行），不是
「历史累积得太多」；**要不要取回那段内容，决定权交还给模型**。定位符**不透明**：消费方只按取回指引
渲染，不假定它一定是文件路径（本地后端的定位符是一条路径，取回指引让模型用 `read` 打开它——`read` 就是
取回路径，所以它自己的答案**不再被 spill**，否则会把要取的东西又埋掉）。本地后端写入是**排他**的
（`CREATE_NEW`：预先埋下的符号链接既不跟随、也不写穿），且**尽力而为**：写不进去就保留内联原样，绝不把
一次成功的调用变成 `isError`。
*别叫成* `harness.cap.jobs/spill!`——那是**前台命令**答案超预算时落成一份记录（`bash` 的一次回答，存在
jobs 树里），是同一个动词的**另一种** spill；两者目录、id、答案形状都不同，别混。
**三档分工**：spill 管**单条刚产生的**工具结果；剪枝管**已有的大**工具结果；摘要管**一整段**历史；
**信封**（system prompt + 工具 schema）三者都不管。
**轨迹** —— 一条会话的**记录**按**轮**读回来的东西：那一轮模型手里到底有什么（system 消息的字节、
拼在它旁边的上下文、每条用户消息、每次工具调用的参数与结果），以及每次调用发出去的工具表。
**它如实还原记录里的 `message` 行，不隐瞒也不转换**：某个 run 写了行就画，同一段注入再次出现就再画
一次；每一格的**样式**（`system` / `context` / `user` …）是读法，**内容**永远是那些字节。
读法住在 `harness.edge.trajectory`，从管理边由 `GET /api/threads/<stem>/trajectory` 吐出去。
它读的是**记录**不是状态，所以**不进库**；它与**会话统计**是两个读者、读同一份文件的两半
（统计数数，轨迹看内容），也与客户端手里那段窗口不是一回事——system 消息与注入的上下文
客户端从来没有过。
*别叫成* trace / telemetry / span / 时间线（那只是它的画法之一）。

**附件**（attachment）—— composer 里挂上、**随这条用户消息一起**发出去的一份图（今天只有图片这一种，
`image/*`）。它**不是一个上传**，这是这个词最要紧的一半：本仓没有上传这条路——没有收字节的端点、
没有中间存储、没有 URL、没有生命周期。字节以 data URL 的形式**躺在消息里**：客户端把文件读成 data URL
（`lib/attachments.ts`），服务端把它翻成厂商的 `image_url`
（`harness.edge.ag-ui/provider-image-url`）。所以它**不进库、不落盘、不从库里读回来**；记录里它跟着
每一轮的 `input` 行被重记一遍，那就是它留下的全部痕迹（2 MB 的上限就是为这件事立的）。
*别叫成* 上传 / upload（那是本仓没有的一条路）、文件（file 是这一份东西的统称，附件是消息里的那一份）。

**任务清单** —— 本会话的待办，由 `todo_write` 写。一次调用送的是**完整清单**，不是增量：整份替换
（空数组即清空），顺序本身就是信息。**正因为它每次被整份改写**，它是状态而不是记录，所以它进库
（`todos`：一个会话一行，清单整存整取，读它的人渲染整个列表）。一次模型调用只写一次——两条
"整份替换"之间不存在合并，而并发跑会让后写的赢、两个都报成功。
*别叫成* TODO list、待办、task list（中文一律写"任务清单"）；也**别叫成「任务」**——
那个词在本表里是**一段没有家的会话**，两者不是一个东西。

**冻结的开头** —— `prompt.md` 是**一条 system 消息的开头**，不是整条消息。它装的是**承诺**：与任何
会话都成立的话（身份、secrets 纪律、「其余自己读」），首调读入即冻结（provider 前缀缓存的前提），
热改要 `(harness.kernel.llm/reset-prompt!)` 或重启。消息的其余部分是**本会话的事实**（工具集合、绑定的目录、
provider 档），由 `harness.cap.system-prompt/assemble` 在每次 run 现算——事实能在会话中途变，冻下来的那句
就会说一件已经不成立的事。它是**代码资产**：进 git、要人 review，是唯一一个不住在配置家里的配置文件。
**因为冻结，它不按模式拼两套文本**——锚点语法归工具描述讲（工具描述是 per-thread 的，可以随模式变），
prompt 只中立地点出自省入口。
*别叫成* system prompt（那是整条消息，头只是它）、冻结的 prompt。

**围栏**（fence）—— 绑定了项目的会话里，文件工具可以直接碰、不必 park 的那些目录：项目目录自己
（除非 `:approval {:strict true}` 把它拿掉）、配置家、本机临时目录（`java.io.tmpdir` 与 POSIX `/tmp`）、
技能根（即「技能层」那两档所在的目录）、`:approval {:allow [..]}` 声明的路径。
它**只有一个来源** `harness.cap.project/fence`，因为它是同一件事的两面：门禁拿它判要不要 park，
`<project>` 块拿它对模型说规则——两处若是各写一份，模型就会以为某个路径自由而实际被拦。

**park / 审批** —— 一次工具调用在跑之前被挂住等人决定，那个人的决定一次性取用。这是**能力**边界，
不是编辑纪律：编辑模式从不用 park 表达自己。

**hook** —— 一个命名的 hook 点加一条**声明**，声明说它跑什么：`:command`（shell 命令，经 stdin 拿
这个点的 payload）或 `:run`（本进程里的可调用），**恰好一样**。点表 27 行是数据。来源有三个，
先后按这个档位定：`:built-in`（内核自己注册的行）、`:config`（配置家的 `hooks.edn`，叠在绑定项目的
`.harness/hooks.edn` 上）、`:session`（本会话自己的 overlay）。退出码决定放行还是阻断；`SystemPrompt`
那一点例外——它的 stdout 是**内容**（追加进 system 消息的文本）。本会话可以给自己加、撤、关、开 hook
（走 `eval`），进程重启即失。
