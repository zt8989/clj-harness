.env
.cpcache/
.nrepl-port
target/
node_modules/
ui/dist/
ui/cljs-out/
ui/.shadow-cljs/

# Local agent tooling: browser-driver artifacts and WorkBuddy's per-project
# memory. Both are machine-local working state, not source.
.playwright-mcp/
.workbuddy/
