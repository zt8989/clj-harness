# 05 — 从全部记录重建库（灾难恢复 + 启动追赶）

**What to build:** 一条路径遍历**所有**记录、重建可投影的状态：启动时若某个会话的 offset 落后就**增量追赶**；库缺失或损坏（本仓真发生过——库被隔离重建成空）时，用一个显式入口做**全量重建**。判据场景：删掉 `harness.infra.db`、重启，项目 / 会话 / 标题 / 上次发送时间 / 任务清单原样回来。**不假装能重建不可投影的那几样**（锚点归属与快照、claim 行）——它们随库一起丢，票 02 已经把这份名单写下来，这一票把它原样映进代码与文档。重建是幂等的：同样的记录跑两遍，库相同。

**Blocked by:** 03 — 消费者是这条重建的路基。

**Status:** ready-for-agent

- [ ] 删掉库后重启 ⇒ 可投影的状态原样回来（项目、会话、标题、上次发送、待办）
- [ ] 落后时**增量**追赶，不全量重放；有断言证明只读了 offset 之后的行
- [ ] 重建幂等：跑两遍，库的指纹相同
- [ ] 不可投影的状态在代码注释与文档里被点名「随库丢失」，并有一条用例说明它们**不会**被伪造出来