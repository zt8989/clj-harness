# 06 — 收口：文档与 prompt 跟上「工具来自外部服务器」

**What to build:** 仓库里关于「工具有哪些、从哪来、配置放哪」的每一处说法与真实情况一致：home 的目录清单
多了 `mcp.edn`，架构段多了 `harness.mcp`，多了一节讲 MCP 的装配与契约，工具表那句枚举也已经不再是真的。
这一票是扇入点：01–05 都落地了，现状描述才真的过时到可以一次改对的程度。

**Blocked by:** 01, 02, 03, 04, 05

**Status:** done（2026-09-16，分支 `mcp`）

## 验收

- [x] README 的配置家目录清单加上 `mcp.edn`（含它「可以不存在」这条：不存在 = 一个服务器都没声明）
- [x] 架构段的 `src/harness/` 清单加上 `harness.mcp`，一句话说清它是什么（客户端生命周期 + 工具桥接 +
      elicitation 接入），并指向本特征的 spec
- [x] 新增一节「MCP（`mcp.edn`）」，写清：两级装配与整表替换、server 名规则与 `mcp__<server>__<tool>`、
      工具定义带 `:source`、**超时与重连**、**关闭不是隐藏**的语义、`mcp/server` 审计行的存在与用途、
      两条管理端点、以及 `:env`/api-key 那条「永不入日志与端点」的纪律
- [x] **`prompt.md` 那句工具枚举不再枚举**：`Tools: read, write, edit, bash, eval.` 在外部工具进表那一刻
      就必然过时。改成不枚举的说法，并加一句让模型知道 `mcp__<server>__<tool>` 这类工具是**外部程序**提供的
      （来源不同，失败的样子也不同）——这是 prompt.md 的一次改动，按它的流程走（改完 `reset-prompt!` 或重启）
- [x] 全仓搜一遍「六个内建」「six built-ins」那类把工具数写死的说法（`src/` 注释、`test/`、README、
      `.scratch/*/spec.md`），逐处判断是历史记录还是现状描述——**历史记录留着，现状描述改掉**
- [x] `.scratch/general-harness/spec.md` 里 P1 的两条（工具来源泛化、MCP）标注为已由本特征交付，
      并把两处**收窄**写在那里：不做「按工具配审批策略与超时」，v1 只桥接 tools
- [x] 验证段的测试基线更新为实际值（离线全量与 UI 两处）
- [x] `clojure -M:test -m harness.test-runner` 全绿；`cd ui && npm test` 全绿；`cd ui && npm run build` 全绿
- [x] 真 Chromium 把本特征各票的界面结论（MCP 面板三态）再走一遍，截图留档

## 复议（2026-09-16，`system-prompt-blocks` 落地时加，原文不动）

上面第 4 条验收要求把 `prompt.md` 那句工具枚举**改成不枚举的说法**。**不必了，而且比那个更强**：
`system-prompt-blocks` 让枚举**由构造正确**——`<tools>` 块在每次 run 组装 system 消息时从活的
工具表派生（会话注册的、关掉的、以后 MCP 桥进来的都在里面，还专门有一行说清哪些来自**外部程序**），
而 `prompt.md` 里那句枚举已经整段退场。

所以这一条落地时请**直接删掉**，不要再去写一句「不枚举」的话：**一句话都不写**比「写一句正确的空话」
更符合这个特征。其余各条不受影响。

## 落地（2026-09-16）

**一处与票面不同，且是照着后来定下的规矩改的**：票面说「把 general-harness spec 里 P1 的两条标注为已交付」，
但**`.scratch` 是历史、不再修改**（那是后来明确的一条纪律）。所以现状写进了 `docs/architecture/`，
而不是回头改那份 spec。要对照的读者从 mcp 一页往上找即可。

- **README**：配置家目录清单加 `mcp.edn`（三处提到「现读的文件」的地方一并跟上）；新增
  「MCP 服务器（可选）」一节（两处声明与二选一、整表替换、命名、同一个执行缝、`:env` 纪律、
  坏服务器不拖死任何人、设置页里的账本与开关），细节指向 `docs/architecture/mcp.md`；验证段的
  基线改成本分支的实际数，并把那两条既有的红**写在原地**（原来写「见下」，而「下」那一节已经被删了）。
- **新增 `docs/architecture/mcp.md`**：现状一页——声明与校验、连接的身份与三条失效路径、
  按树收尾、超时丢连接、一次调用的路径、elicitation 的完整回路与它**明知要付的价**（调用被重新发起，
  所以服务器会再问一次；HTTP 上不接线）、三条管理路由、四个状态词、`mcp/server` 行的落点与
  「只有变化才落行」、界面的位置，以及代码在哪。
- **架构索引**：模块地图加 `mcp` / `parked` 两行；章节加第 9 页（并重排编号）；「在办」一节把 MCP
  从「代码里一行都没有」改成「在分支 `mcp` 上，落地后 main 会多出什么」。
- **`edge.md`**：路由表加三条；审计行清单加 `mcp/server`。
- **`home-and-storage.md`**：两级装配的表里，把 `mcp.edn（未落地）` 改成果真。
- **`hooks.md`**：已接线从 7 变成 **9**（`Elicitation` / `ElicitationResult`），未接线的数跟着减，
  并写明这两个点是观察者、以及为什么答复通道不给它们。
- **`client.md`**：组件清单加 `mcp-panel.tsx`，并说明它嵌在设置页里。
- **`prompt.md` 一个字没改，因为它已经不需要改**：票面要求「工具枚举不要枚举了」，而
  `system-prompt-blocks` 那次收口已经把它删光了（现在只有 25 行：冻结开头 + secrets 纪律），
  工具是运行时由 hook 拼进系统消息的——而那个 `<tools>` 块**本来就按名字前缀把 `mcp__*` 标成
  「借来的手」（external: an MCP server provides these, not this kernel）**。这条是构造上就满足的，
  票面按旧印象写，这里如实记一笔。

**没做**：全仓搜「六个内建」那类把工具数写死的说法——搜下来只剩历史票面与 spec 里的（那些不改），
`src/` 里没有断言「六个」的地方，工具数由表自己决定。
