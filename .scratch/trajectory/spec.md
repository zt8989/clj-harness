# spec: 轨迹 —— 在对话之外，把这条会话照记录读回来

参考：DeepSeek Harness 的「轨迹」页签（`对话` / `轨迹` 两个页签；上方一条 `输入` / `模型` / `工具` 的时间条；
左侧按轮列出每一条；右侧看那一轮的 system 提示词与工具定义）。参考的是**要回答的问题**，不是像素。

**一句话**：`对话` 回答「我们聊了什么」；`轨迹` 回答「模型每一轮到底看到了什么、花了多久」。
它读的是**记录**（`~/.clj-harness/logs/<会话>.jsonl`），不是客户端内存里的那份对话。

## 决策

1. **数据来源是记录，读侧在后端。** 要显示的东西——system 消息的字节、工具表照发出的样子、
   每次调用的用量与耗时——客户端一个都没有，AG-UI 帧里也不该有（帧是给对话用的）。所以：
   后端把 jsonl 折好，视图只画折好的东西。视图不自己解析日志，也不为了它给协议加字段。

2. **折的单位是「轮」与「模型调用」，两个词进 `CONTEXT.md`。**
   *轮* = 一条用户消息，以及它引起的全部输出（可能含多次模型调用）；*模型调用* = 一次
   `llm/stream!`，加上它为它要的那批工具调用。
   轮**不等于** `input` 行：悬置恢复会产生第二个 `input`、两次共享同一个 `runId` 且**没有新的用户消息**
   （`bb89705c…` 那次日志里就是），那仍然是同一轮。

3. **判「本轮新增了什么」用消息 id 的差集，不用内容的公共前缀。** 客户端每条消息带 id
   （`input.messages[].id`），恢复时它会**重发全部历史**（`harness.replay` 的 docstring 已经这么说了）。
   内容比较会被两件事咬到：同一条用户消息发两遍、以及 system 消息会变（见决策 9）。

4. **读侧容忍半截的 run。** `harness.replay/ensure-complete!` 把「最后一个 run 没有终帧」判为截断并拒绝
   ——那对（重建不能重建半个对话），但轨迹最常被打开的时刻正是**会话正在跑**的时候。
   所以轨迹的读侧不复用它：读到哪算哪，并在返回值里如实标出最后那一轮还没结束。

5. **新的记录是审计行，不是帧。** 两条：`model/start` / `model/end`，走
   `harness.http/lifecycle-record` 那条既有的「内核事件 → jsonl 行」的缝（`:tool/pre-execute` 那三条就是这么来的）。
   **AG-UI 帧一个不加**，客户端在对话里一个字都看不到——协议不动是这个仓库的硬约束，别顺手加帧。

   **缺什么就补进 jsonl，只有这一个去处**：不落表（记录不是状态，`harness.db` 的边界与 `db_test`
   的两条元断言都拦着）、不进帧（协议不动）、不在客户端编。视图上任何一格，
   要么指得到记录里的某个字段，要么就是本特征**新补的那两行**——不许有第三种来源。
   **下面的「记录清单」是这张账**，它是**闭的**：清单里没有的格子不许画。

6. **工具表在同一次 resolve 里既进请求体、又进标记。** `harness.llm` 的 `stream!` 今天在
   `(tools/specs thread-id)` 的结果上现拼 body；把它绑一次，那一份同时进 body 与 `model/start`。
   于是「照发出的样子」是**构造正确**的，不是「再解析一次、碰巧相等」。

7. **时长由 `:ts` 差出来，不加新的时间戳字段。** 每条 jsonl 行本来就有 `:ts`：
   `model/start`→`model/end` 是一次模型调用，`tools/pre-execute`→`tools/post-execute` 是一次工具执行。
   悬置的等待是**另一段**（`tools/pre-execute` → `approval/decided`），不算进执行时长——
   一次等人的审批不该长得像一次跑了三分钟的工具。

8. **表达**：`轨迹` 是**页签/视图**的名字，它读的东西叫**记录**。代码里不要叫 telemetry / trace / span——
   `CONTEXT.md` 已有 会话 / 帧 / 记录 三件套，多一套近义词就是多一次改名扫描。

9. **system 消息按轮显示，变了就得看得见。** left 列表里 `系统` 条只在第一轮出现（标 `初始系统提示词`），
   此后**只有在字节变了的那一轮**再出现一次；右侧面板永远显示**选中那一轮**的 system 字节。
   这条是为在办的 `system-prompt-blocks` 写的：那一票落地后同一会话的 system 消息会逐轮不同，
   轨迹不解析块的来源，只如实显示字节与「它变了」。

## 记录清单（视图的每一格从哪来）

**这张表是闭的**：左边每一格都必须在右边有一个来源，来源只有两种——**记录里已经有**，或者
**本特征补的那两行**。要画一格而表里没有它，先补记录再画；不许在客户端算，不许拿别的行凑，
不许用今天的值冒充当时的。落地时（07）逐行核一遍。

| 视图/读侧要的 | 读哪一行、哪个字段 | 今天 |
|---|---|---|
| 系统条的字节 | `message` 的 `role: "system"`（每 run 一份，逐字节） | 有 |
| 上下文条：指令文件、技能清单、技能正文、本 run 的 context | `message` 的 `role: "user"` 里**不属于**该 run 自己 `:brought` 的那些（该 run 自己写下的行）。**逐字节如实**（2026-09-24 主人定：轨迹要真实还原 jsonl 的 kind=message，不隐瞒、内容不转换）：某个 run 写了行就画，两个 run 都写了就画两次；带 id 的出生条目（开场块、session context）只有出生那一轮有行，就只画那一轮 | 有 |
| 用户条 | `input` 的 `:messages`（带 id —— 判「本轮新增」就靠它） | 有 |
| 助手文本与推理 | `message` 的 `role: "assistant"`（`content` / `reasoning_content`） | 有 |
| 工具的名字与参数 | 上一条的 `tool_calls[].function`（`arguments` 是原样的 JSON 串） | 有 |
| 工具的结果与错误 | `message` 的 `role: "tool"`；错误另有 `tools/execute` 的 `:error` | 有 |
| 工具的三态与「有没有真的跑」 | `tools/pre-execute` 的 `:outcome` / `:missing`；`tools/execute` 的**有无** | 有 |
| 工具段与等待段的时长 | 三行 `tools/*` 与 `approval/decided` 的 `:ts` | 有 |
| 模型段的起止与时长 | `model/start` → `model/end` 的 `:ts` | 标记已在（`composer-status`）；**读侧的段仍开着**（05） |
| 用量、结束原因、vendor 回声的 model | `model/end` 的载荷 | ~~补（05）~~ 有（`composer-status` 已落地） |
| 工具表照发出的样子 | `model/start` 的 `:tools`（与进请求体的是**同一次 resolve** 出来的那个值） | ~~补（04）~~ 有（2026-09-17 落地） |
| 这次调用发给谁、用什么参数 | `model/start` 的 `:model` / `:base-url` / `:reasoning-effort` | ~~补（04）~~ 有（`composer-status` 已落地） |
| 这次调用属于哪一轮、是第几次调用 | 数序（见下），不另记 | 派生 |
| 这一轮是谁的、会话绑到哪 | `project/bound`；`provider/init` / `provider/changed` | 有（保留，不重复记） |
| 每一格发生的时间 | 每一条 jsonl 行自己的 `:ts`（不新记时间戳字段） | 有 |

**留在派生里的只有四样：轮边界、轮序号、调用序号、等待段。** 它们不是缺口，是**不该记**：
记一份就等于给同一件事实留第二份，两份必然会漂（`CONTEXT.md` 的「库不镜像日志」是同一个道理）。
派生住在读侧，而且必须**能用手搓的记录断言**（02 的验收就是这条）。

## 非目标

- **不落 sqlite 表。** 轨迹是记录（只追加、不改写），按 `harness.db` 的边界与 `db_test` 的两条元断言
  （表名白名单、列名禁止 `messages?|events?|frames?|contents?…`），它不进库。
- **不改 AG-UI 协议**：不加帧、不改任何客户端已经收到的东西、不动 interrupt/resume 的形状。
- **不做实时流式轨迹**：打开时取一次、每次 run 结束时再取一次，正在跑的轮按已完成的部分画。
- **不记请求体的 `messages`**：条本身就是它们，再记一份就是同一件事实的第二份。请求的**参数**
  （model / base-url / 思考档）随 `model/start` 记——它们不是 body 的拷贝，是这次调用的身份。
- **不做成本/计价推算**（只报 vendor 给的用量），不做跨会话聚合、对比、导出。
- **`调用` 那种「每次调用等宽」的看法，以及按文本高亮，先不做**——那是画法不是数据，要就再来一张票。
- **不给 `:reasoning` 单列一条**：请求体里没有它（`harness.ag-ui/inbound` 把它并进助手消息），
  它挂在助手条上（`reasoning_content`），如实即可。

## 验收主线

一条会话，跑三轮（含一次工具调用、一次悬置否决）：

1. `GET /api/threads/<stem>/trajectory` 返回三轮；轮内每条的**顺序**与模型当时看到的顺序一致：
   `系统` 在最前，开场块与本 run 的 context 标成 `上下文`，工具条带参数与结果。
2. 右侧面板读到的那一轮 system 字节，与日志里 `role: "system"` 的 `message` 行**逐字节相等**。
3. 右侧「工具」页签列出的表，与 `model/start` 行里的表**逐字节相等**——它就是发出去的那一份。
4. 时间轴上 `输入` / `模型` / `工具` 三条 lane 有正时长的段，且**每段与左侧一条对应**（同一份数据两种画法，
   不第二次折日志）。
5. **对照**：加了 `model/*` 两条审计行之后，`harness.wire` 的帧校验全过，`harness.frames` 折出来的对话
   与今天**逐字节不变**，`harness.replay` 的读侧一行不用改。
6. 打开**正在跑**的会话不报错，最后那一轮如实标成未结束。

## 跨特征对照

- **`jsonl-message-record`（已落地）**：本特征**依赖**它——没有逐条 `message` 行就没有「条」。
  新增的两条行种类不改它的读侧（`replay` 只认 `input` / `event` 两种行）。
- **`system-prompt-blocks`（在办）**：那一票落地后 system 消息 = 冻结开头 + 各来源追加的块。
  本特征显示的是**读回来的那一份字节**，所以两边同时落地不打架——反倒正是它让「system 消息会变」成为真事
  （决策 3、9 就是为它写的）。轨迹不解析块的来源，也不给块单独分类。
- **`composer-chrome`（已落地）**：视图切换与它没有交集；模型/思考选择器改变的是 provider，
  轨迹只是把变化如实记下来（换 model 那一轮，`模型` 段的 model 名会不一样）。
- **`model-limits`**：若落地 `max_tokens` 之类，它们进请求体；本特征不显示请求体（非目标）。
- **`hook-engine` / `action-fusion`**：`eval` 内层调用的相位事件将来若落地，是往同一条审计缝上**加行**，
  不改本特征的折法；本特征按 `tools/*` 与 `model/*` 画，不预判内层调用。
- **`docs/architecture/edge.md:103`** 今天写「读日志的代码只认 `input` / `event` 两种行；其余是审计轨迹」——
  本特征让读侧多认几种行，那句要改（落在 07）。

## 交付顺序

这条链是**串的**，不是把它切粗了：01 立词；02 把记录读成轮（谁都要用它）；03 才有那张脸
（右侧面板与轮的选择在这里）；04 是**请求侧**（表照发出的样子，挂在 `model/start` 上）；
05 是**响应侧**（用量与耗时，挂在 `model/end` 上，并给每一段补时间）；06 画时间轴（段来自 05）；
07 收口。

`02 → 03 → 04 → 05 → 06` 每一步都真的挡住下一步：没有 `model/end` 就没有段，没有段就没有时间轴；
表要挂在那条 `model/start` 上，所以它只能排在 04。**不为了「有并行边」把它们并成一张大票**——
并起来就是一次要在一个上下文窗口里做完记录、读侧、视图与画法。

## 票清单（`.scratch/trajectory/issues/`）

| # | 票 | Blocked by | 交付什么 |
|---|---|---|---|
| 01 | 词：轮 / 模型调用 / 轨迹 | — | ~~`CONTEXT.md` 立词；顺手把 `批` 那条里的「回合」改成「模型调用」~~ **见文末复议：`轮` / `模型调用` 与 `批` 那条已由 `composer-status` 01 落地，本票只剩 `轨迹` 那个词** |
| 02 | 轨迹的读侧与端点 | 01 | ~~折法 + `GET /api/threads/<stem>/trajectory`；只到边，没有界面~~ **2026-09-17 落地（ns 是 `harness.edge.trajectory`），见文末落地记录** |
| 03 | `对话` / `轨迹` 切换与逐轮条列 | 02 | ~~页签、视图、左侧逐轮条列、右侧 `系统提示词` 页签、真机证据~~ **2026-09-17 落地**（切换用普通按钮，没取注册表的 `tabs`；见 03 复议） |
| 04 | 请求侧：调用的边界与照发出的表 | 03 | ~~`model/start` / `model/end` 两条审计行~~ 已由 `composer-status` 01 落地；~~`:tools` 与 `工具` 页签~~ **2026-09-17 落地，本票结束** |
| 05 | 响应侧：用量与耗时 | 04 | ~~留住 usage / finish_reason / 回声的 model~~ 已由 `composer-status` 01 落地；~~读侧的段、用量与行上显示~~ **2026-09-17 落地，本票结束**（时间只留在 `calls[]` 一份，条目带 `:call` 指针） |
| 06 | 时间轴：三条 lane | 05 | ~~`输入`/`模型`/`工具`；`时长` 与 `轮次` 两种看法；搜索~~ **2026-09-17 落地**（等宽几何量过：0% / 33.33% / 66.67%） |
| 07 | 收口：现状与全量验证 | 04, 05, 06 | ~~`docs/architecture` 与 README 跟上；两套全量 + 真机证据~~ **2026-09-17 落地**（804 / 11028 / 基线两条；前端 24 全过；真机 4 张） |

## 状态

~~未开工（票已按仓库约定立在 `issues/`，全部 `ready-for-agent`）。~~
**部分开工，见文末复议（2026-09-16）：记录侧那半已由 `.scratch/composer-status/` 01 落地，本特征本体的视图仍未开工。**

基线（立票当日实测）：`main` @ `63869d2`，`clojure -M:test -m harness.test-runner`
→ `Ran 569 tests containing 9574 assertions. 0 failures, 0 errors.`，退出码 0。
前端 `cd ui && npm test`（`EXPECTED_CASES` 仍是 11，本特征不改它）与 `cd ui && npm run build`。
报数一律带上**分支与提交**（基线随分支变）。

## 复议（2026-09-16）：记录侧由 `composer-status` 先落地

**这是加注，不是改写。** 上面那些话是立票当天的事实，留着；这一节说的是后来发生了什么。

**发生了什么**：`.scratch/composer-status/`（牛总 2026-09-16 立，4 张票）要在 composer 下面画一条状态条，
而它要的五个数——几轮、几步（**模型调用**）、输出速度、总用量、缓存命中——与本特征的**记录清单**是同一批格子。
它比本特征小、要得早，所以记录侧由它先补（牛总当场裁定）。

**本 spec 的 01 / 04 / 05 各缩水成什么**：

| 本 spec 里的 | 今天的归属 |
|---|---|
| 决策 5 的两条审计行 `model/start` / `model/end` | `composer-status` 01 落地：行名与载荷字段**一字不改**（`model/end` 带 `{:usage … :finish-reason … :model …}`，厂商的键名逐字）。**04 只剩往 `model/start` 上挂 `:tools`** |
| 05 的 `consume-sse` 留住 `usage` / `finish_reason` / 回声的 `model` | `composer-status` 01 落地：`stream!` 的返回形状随之变成 `{:message … :telemetry …}`（四个 defmethod 一起改） |
| 01 的 `CONTEXT.md` 立词 | `轮` / `模型调用` 与 `批` 那条的「回合 → 模型调用」已由 `composer-status` 01 落地。**01 只剩 `轨迹` 那个词**（仍是它的） |
| 02 的读侧 ns 名 `harness.trajectory` | **这个名字与 `layer-layout`（已落地）之后的分层对不上**：记录读侧今天是 `harness.edge.replay`，帧读侧是 `harness.kernel.frames`。`composer-status` 02 的聚合读侧落在 **`harness.edge.stats`**；本特征那半个读侧应落在 `harness.edge.trajectory`，且**轮的判据复用 `edge.stats` 里那一个实现**（不许写第二份） |
| 07 的「`edge.md` 那句『读日志的代码只认 `input` / `event` 两种行』要改」 | 仍由 07 改：今天的读侧还没读 `model/*`，那句话此刻**还是真的** |

**一字未动的**：`轨迹` 那个视图、轮的折法（消息 id 差集）、`模型调用` 的定义、记录清单是闭的、
「记录不是状态、不进库」、AG-UI 协议一个字不加——`composer-status` 是照着这些做的，不是另立一套。

## 落地记录（2026-09-17）：02 落地，04 的记录一半补齐

**这仍是加注。** 上面两节都留着；这一节只记今天真做了的事。

**分支 `trajectory`**（worktree `.worktrees/trajectory`，从 `main` @ `f7f4d31` 切出）。

**落地的**：

- **01 收尾**：`轨迹` 补进 `CONTEXT.md`（挨着 `会话统计`，写明它俩是同一份记录的两个读者）。
- **02 全部**：`harness.edge.trajectory` —— 记录按轮折回来，`GET /api/threads/<stem>/trajectory`
  从管理边吐出去，`thread-verbs` 闭集加第四个词（`stats` 已经是那条形状上的第一个 GET，
  「每一个动词都是 POST」那句话上一轮就改过了）。
- **04 的记录那一半**：`model/start` 多一个 `:tools`。`harness.kernel.loop/model-call!` **resolve 一次**，
  同一份值既进请求体（provider map 上的 `:tools`）又进标记；`harness.kernel.llm` 不再自己 reach 工具表。
  「照发出的样子」因此是**构造正确**。读侧轮里长出 `:calls [{:index :model :tools}]`。
- **一处既有事实两写被收掉**：`harness.edge.stats/incomplete?` 提成公开——两个读侧共用同一条
  「这份日志完了没有」（这条正是上一节预判的那句「轮的判据复用同一个实现」的近亲）。

**仍然开着的**：03 的视图与右侧面板（含 04 的 `工具` 页签）、05 的读侧段与耗时、06 的时间轴、07 的收口。

**验证**：`test/harness/edge/trajectory_test.clj` 13 条（手搓记录断言折法 + 真 HTTP 打端点）。
全量套件与基线的逐条对照见 07 —— 本机已知的红是 `harness.cap.project-test/a-binding-survives-a-real-restart`
（fork 子进程，基线上同样红），以及 `harness.edge.http-test` 依赖磁盘 mtime 的那两条**时红时绿**。

## 落地记录（2026-09-17，收口：全部七张票）

**分支 `trajectory`，从 `main` @ `f7f4d31` 切出，工作树 `.worktrees/trajectory`；改动尚未提交。**

**做完的**：01 词（`轨迹` 补进 `CONTEXT.md`）、02 读侧与端点（`harness.edge.trajectory` +
`GET /api/threads/<stem>/trajectory` + 14 条测试）、03 视图（切换 / 逐轮条列 / 右侧 `系统提示词`）、
04 的记录与 UI 一半（`model/start` 上的 `:tools` + `工具` 页签）、05 读侧的段与用量、
06 时间轴（三条 lane / 两种看法 / 搜索）、07 收口（文档 + 全量 + 真机）。

**落地后与票面不同、且值得记下的**（细节在各自的复议段里）：

- ns 是 `harness.edge.trajectory`（分层之后读侧在 `edge`）；「第一个 GET」早已被 `stats` 占掉。
- `:calls`、工具表、`stats/incomplete?` / `user-ids` / `tokens-of` 的公开，都是落地时按「一件事一个地方」
  定的：轮里只有**指针**（`:call`），时间只有 `calls[]` 那一份，规则只有一份实现。
- 视图切换**没有**用注册表的 `tabs` 原件，两个信号读在视图体内（`App` 里读会抛）。
- 「每次调用等宽」的第三种看法没做（非目标照旧）。

**验证**：`clojure -M:test -m harness.test-runner` = **804 / 11028 / 基线那两条**；
`cd ui && npm test` = **24 全过**；`cd ui && npm run build` 过；真机 4 张在 `evidence/`。
**唯一没有验到的**：真 vendor（全程用脚本替身，与这个仓库的既有纪律一致）。

## 复议（2026-09-17 晚）：右侧从「固定两页签」改成「点哪条展哪条」

**牛总当场改的口径**：右侧不该常驻显示 `系统提示词` / `工具`，而应**跟着点击的条目展开、默认不显示**。
按这条改，**记录清单一个字没动**（它记的是数据，这条改的是画法）：那些格子照旧在，
只是入口从「一轮的两个页签」变成「那一条自己」——system 消息是 `system` 条，
工具表是那次调用 `assistant` 条上的 `:call` 指过去的。验收主线第 2、3 条因此读作
「点开 `system` 条 / 点开某次调用的 `assistant` 条」，字节对照不变。

## 复议（2026-09-18 凌晨）：`tools/*` 那三行的「时长」不是我以为的那三段

记录清单里「工具段与等待段的时长 ← 三行 `tools/*` 的 `:ts`」这一格，**格子没错，读法错了**：
`tools/execute` 是**离开**执行（工具跑完的那一刻），不是开始。于是「工具那段」应当是
`tools/pre-execute`(到达) → `tools/execute`(跑完)，而悬置的等待要用**第二次 pre-execute**
（恢复）来切。照原读法画出来的工具条只有 2 毫秒，真正跑的时间被误当成「等人」——这是这条时间轴
第一版最实在的一个错，靠牛总一句「input 和 model 之间那段空白没标出来」才发现。
结论一句话：**这四行记录的是四个时刻，不是「开始/结束/闭合」三段**。
