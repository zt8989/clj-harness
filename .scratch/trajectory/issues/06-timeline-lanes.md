# 06 — 时间轴：`输入` / `模型` / `工具` 三条 lane

**What to build:** 轨迹视图上方一条时间轴，把左侧那份数据的**时间维度**画出来：三条 lane，
每条 lane 上的段与左侧的行一一对应。另加一个搜索框过滤左侧的条目。

从用户视角：一眼看出这一轮慢在哪——是模型想得久，还是工具跑得久，还是卡在等审批；
再点那条段，落到对应的那一行上。

**Blocked by:** 05（段与用量是那一票给的）

**Status:** ready-for-agent

## 验收

- [ ] 三条 lane，与参考图同构：`输入` / `模型` / `工具`。
      `输入` 每轮一个**细刻度**（该轮第一条用户消息那个时刻——用户输入是一个点，不是一段）；
      `模型` 每次模型调用一段（`calls[]`）；`工具` 每次工具执行一段。
- [ ] **同一份数据的两种画法，不第二次折日志**：段全部由 05 已经折好的字段画出来，
      条带里不许再解析一次记录，也不许自己算一遍时长。
- [ ] 段与行**互相认得**：悬停/选中一个段高亮左侧那一行，反过来也一样，点段滚到那一行。
      这条同时是本特性的自查——两处对不上就是折法错了。
- [ ] **并发的工具在 lane 内纵向堆叠**：同一次模型调用要的多个工具是并发跑的，段会真的重叠；
      lane 的高度按最大并发生长。**不把它们排成首尾相接**——那会把真实的等待画没了。
- [ ] 被 park 的工具：等待画成执行段**前面**的浅色前段（`queuedAt` → `startedAt`），
      视觉上与执行分得开。
- [ ] 被否决的调用（从没执行）：画成一个**没有时长的记号**（空心），不是一根 0 宽的段——
      它确实发生过，但没花时间跑。
- [ ] 两种看法（`时长` / `轮次`）：`时长` 是真实时间轴（最早到最晚）；`轮次` 里每轮**等宽**、
      段按轮内相对时间铺开，于是条带的轮与左侧的分组对齐。
      **第三种（`调用`，每次调用等宽）本票不做**，理由写在 spec 的非目标里：那是画法不是数据。
- [ ] 搜索框过滤左侧条目：只搜**条目自己**的文本（含工具参数与结果），**不搜 system 条的正文**——
      搜出来的每一行都得是能点开看的证据，system 全文在右侧面板里（否则每次搜索都命中同一个系统提示词）。
- [ ] 老日志（没有 `model/*` 行）：`模型` lane 空着并**如实标注**（那一轮的记录里没有模型段），
      不拿别的间隔冒充。半截的 run（端点 `incomplete`）画到最后一个已知的段为止。
- [ ] 没有可画的段（空会话）时条带不出现，不画一根空轴。
- [ ] `data-slot` 补齐：`trajectory-lanes` / `trajectory-lane` / `trajectory-segment` / `trajectory-search`。
- [ ] 真机证据：`.scratch/trajectory/evidence/` 补两张——一张有真实耗时的段（含一次并发工具），
      一张 `轮次` 看法（两条以上的轮）。
- [ ] `cd ui && npm test` 全绿（`EXPECTED_CASES` 仍是 11）与 `cd ui && npm run build` 过；
      `clojure -M:test -m harness.test-runner` 全绿（基线以落地当次为准，报数带上分支与提交）。
