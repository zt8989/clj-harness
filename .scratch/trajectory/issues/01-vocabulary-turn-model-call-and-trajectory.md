# 01 — 词：轮 / 模型调用 / 轨迹

**What to build:** 三件事只需要几个字，但后面五张票的标题、测试名、注释、`data-slot` 都要用到它们。
把它先立下来，是**先做 prefactor**：晚一步就是一次改名扫描。

从用户视角：读代码的人（和写代码的 agent）在「一轮对话」与「一次模型调用」之间不会再混；
`CONTEXT.md` 里不会同时存在两个指同一件事的词。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

## 验收

- [ ] `CONTEXT.md` 加 `轮`：一条用户消息，以及它引起的**全部**输出；可能含多次**模型调用**。
      它**不等于**一次 run：悬置恢复会让同一个 `runId` 出现第二个 `input`，那仍是同一轮。
      *别叫成* turn / 回合 / 迭代。
- [ ] `CONTEXT.md` 加 `模型调用`：一次 provider 流式生成，加上它为它要的那批工具调用（那批工具调用是**并发**跑的）。
      *别叫成* step / round / 回合 / LLM call。
- [ ] `CONTEXT.md` 加 `轨迹`：由一条会话的**记录**按轮读回来的东西，是页签/视图的名字
      （对照：`对话` 是客户端手里那份消息序列）。写明它读的是**记录**不是状态，所以它**不进库**。
      *别叫成* trace / telemetry / span / 时间线。
- [ ] 顺手改掉同一份文件里那个不是同义词的第二个名字：`批` 那条的「同一**回合**里对同一个文件的多次编辑」
      ——那里的「回合」就是**模型调用**（一个回合的多个工具调用并发跑，正是那批）。改成一个词，不留两个。
- [ ] 三个词各带上它对着的英文/近义词与**别叫成**那一行，与既有条目同格式（本文件是术语表，
      同义词是要避免的，不是要宽容的）。
- [ ] **一行代码都不改**：本票只动 `CONTEXT.md`。
- [ ] `clojure -M:test -m harness.test-runner` 全绿（基线以落地当次为准，报数带上分支与提交）。
