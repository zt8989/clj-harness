# 03 — `对话` / `轨迹` 切换，与逐轮条列

**What to build:** 03 是这张脸。线程列上方多一个切换：`对话` 是今天这个页面，`轨迹` 换成轨迹视图——
左侧按轮列出模型看到的一切，点一条看全文；右侧面板显示**选中那一轮**的 system 消息字节。

从用户视角：不用 curl 也能看见「模型当时到底看到了什么」；对话页签里永远不会显示的 system 消息与
注入的上下文，在这里逐条出现，且与发出去的字节相同。

**Blocked by:** 02（读侧与端点——视图只画折好的东西，不自己解析日志）

**Status:** ready-for-agent

## 验收

- [ ] `ui/src/components/ui/tabs.tsx` 按 `components.json` 的注册表取原件（registry 是 assistant-ui 那套
      `radix-nova` 风格）。**不改字节就不加标注**；一定要改的，每一处在文件里留 `LOCAL:` 注释——
      注册表原件要保持可与上游逐字节比对，这是仓库的既有纪律。
- [ ] 切换住在 `app.tsx`（我们自己的文件），在线程列**上方**一条；`轨迹` 选中的时候**整列**换成轨迹视图
      ——输入框也一起没有（参考图里也是没有的）。不去改 `Thread` 那个注册表原件；
      真要在它里面插东西，只能走已有的两个自建注入口，并在文件里标 `LOCAL:`。
- [ ] 取数走 `ui/src/lib/trajectory.ts` 一个新模块，照现有 lib 的形状（`AGENT_URL` + `fetch` +
      从 body 里挖 `{error}` 的那个 helper）。**不引入新的取数库**，也不做全局 fetch 封装重构。
- [ ] **取数时机**：视图第一次显示时取一次；此后每次 run 结束再取一次（从运行时拿「正在跑」由真变假
      那个信号，如 `thread.isRunning`），**不加刷新按钮**——少一步是一次胜利。
      正在跑的那一轮按已完成的部分画，末尾如实标出「这一轮还没结束」（端点的 `incomplete`）。
- [ ] 左侧：每个轮一个 `第 N 轮` 的分组（顺序即轮序）；轮内每条一行，行首一个角色标——
      `系统` / `用户` / `上下文` / `助手` / `工具`，颜色与参考图同构（系统与上下文各自可辨）。
- [ ] 行是**一行预览**（截断带省略号），点开看全文：`工具` 行是 `名字 {参数}` → 结果预览，
      参数与结果都来自端点给的原样字符串，不在客户端重新解析或格式化。长文本不截断、可选中复制。
- [ ] `系统` 条只在第一轮出现、标成初始；字节变了的那一轮再出现一次（02 已定的规矩，视图照它画，
      不要自己重算一遍「变没变」）。
- [ ] 右侧面板：`系统提示词` 页签显示**选中那一轮**的 system 字节——等宽、不截断、可复制。
      本票只做这一个页签；`工具` 页签是 04 的。
- [ ] 空态与半截态如实说人话：没有日志、会话跑过但还没有轮、最后一轮没结束，三种文案各不相同。
- [ ] 可见的挂点写成 `data-slot`（如 `trajectory-view` / `trajectory-turn` / `trajectory-item` /
      `trajectory-detail`），给真机截图与将来的调试留位置。
- [ ] 文案与仓库现状同语言：UI 里现有的字全是英文（`New task` / `Send a message…`），
      所以页签是 `Conversation` / `Trajectory`、角色标是 `System` / `User` / `Context` / `Assistant` / `Tool`。
      **这是本票唯一一处「参考图是中文而这里不是」的取舍**：不往 `ui/src` 里塞第一批中文串；
      作者若要中文，改一处常量即可。
- [ ] 证据：真机（Chromium）截图落在 `.scratch/trajectory/evidence/`，至少两张——
      **有内容的轨迹**与**对话页签没变**（切换前后各一张，证明对话那侧一个字都没动）。
- [ ] `cd ui && npm test` 全绿（`EXPECTED_CASES` 仍是 11，本票不改它：套件测的是 AG-UI 那条线，
      视图用真机证据验收）；`cd ui && npm run build` 过。
- [ ] `clojure -M:test -m harness.test-runner` 全绿（基线以落地当次为准，报数带上分支与提交）。

## 复议（2026-09-17，落地）

**落地的**：`app.tsx` 线程列上方一条 `Conversation` / `Trajectory` 切换，选 `Trajectory` 时**整列**
换成轨迹视图（输入框也一起没有）；左侧逐轮条列（角色标 + 一行预览 + 点开看全文，工具行是
`名字 {参数}` → 结果预览）；右侧面板的 `系统提示词` 页签；空态与半截态各自的文案；
取数时机 = 挂载 / 会话变化 / 一次模型调用结束（`assistantCount` 涨）或 run 收敛（`isRunning`），**没有刷新按钮**。

**三处与票面不同，都是落地时才看清的**：

1. **没有从注册表取 `tabs` 原件**。两行切换按钮（视图切换、右面板两个页签）用普通 `<button>` +
   `aria-pressed` 够了，为它拉一个注册表组件要联网取、要把 12 份 `elements/` 之外再多一份需要
   逐字节比对的抄件，而它省下的只是几十行 class。**不加依赖、不联网**是这里更硬的那条。
2. **取数触发不能写在 `App` 里**：`App` 正是渲染 `AssistantRuntimeProvider` 的那个组件，
   在它函数体里 `useAuiState` 会直接抛（浏览器里第一次跑就撞上，控制台原话
   「a component or hook that requires an AuiProvider」）。所以两个信号读在**视图自己体内**，
   与 `ComposerStats` 同一处、同一理由。
3. **右侧面板的默认页签是 `系统提示词`，而 `系统` 条只在第一轮（或字节变了的那一轮）出现**——
   于是选中第二轮以后面板会说「这一轮的记录里没有 system 消息」。这不是 bug，是 02 定的规矩如实显示；
   要看 prompt 就选第一轮。真机证据在 `.scratch/trajectory/evidence/`（4 张，见 07）。

## 复议（2026-09-17 晚，牛总当场改的口径）

**改的是交互，不是数据**：右侧**不再**是「选中那一轮的 `系统提示词` / `工具` 两个页签、常驻显示」，
而是**点哪条展哪条、默认什么都不显示**（再点一次或点 × 收回，宽度还给列表）。

- **布局**：没有选中时**根本没有第二列**（不是空列、不是占位），列表满宽；选中才 `grid` 成 3:2 两栏。
- **`ItemRow` 自己的内嵌展开删掉了**：点一行就是开右侧详情，不再有两个地方显示同一段文本。
- **两个页签连同 `Pane` 一起删除**。它们想显示的两样东西**本来就是条目**，所以都还在：
  `system` 行点开就是那份字节（`initial` / `changed` 由 02 定，视图不重算），
  工具表挂在**那次调用的 `assistant` 行**上（行是这次调用的回答，表是这次调用的请求，`:call` 指针接起来）。
- **详情里的事实不编**：`Facts` 只画记录里有的那几格（`waited` 是 `startedAt - queuedAt`，
  `ran` 是 `endedAt - startedAt`，被否决的调用没有 `ran` 那一格，不是 0）。
- **选中的行会被重新解析**：refetch 之后如果那一条不在记录里了，面板**关掉**而不是继续描述一个不在的东西。

真机证据换成了四张（`t03` 默认无面板 / `t04` 点开一个 tool 条 / `t05` 点开一个 assistant 条看到它发出的表 /
`t06` 对话页签没变），量到的口径：默认 `[data-slot='trajectory-detail']` **不存在**；
点 tool 行 → 事实行 `executed: yes | outcome: pass | waited: 1.2s | ran: <1s | id: call-1`，两个块（arguments / result）；
点 assistant 行 → 该次调用的表 368 行 JSON；再点一次 → 面板又**不存在**。

## 复议（2026-09-17 夜，牛总第二次改口径：工具跟 system 走，且要折叠）

**`system` 那一条现在给两样东西**：那份 system 提示词，**外加一张可折叠的工具表**。
每行一个工具（`🔧 名字 · 描述第一行`），展开是完整描述与该工具**照发出去的整份定义 JSON**
（`{type, function:{name, description, parameters}}`）。折叠用的是**原生 `<details>`**：
没有状态、键盘与读屏免费可用，和 composer 那两个原生 `<select>` 是同一个理由。

- **为什么工具归 system 条**：提示词里那段 `<tools>` 就是这些工具的名字，**工具集变了提示词就变**
  （所以那一轮会冒出一条新的 system 条）。两者本来就一起变，放在一起看才对。
- **按内容分组、带上「哪几次调用发的」**：一轮几乎总是发同一张表，那就一组，
  标签写 `calls 0, 1, 2 sent · 14 tools`；一轮内换过工具就分两组，**差异本身就是要看的东西**。
- **`assistant` 行不再重复那张表**，只留一个计数（`tools: 14`）——同一份 JSON 在两个面板里
  就是多一处要跟着改的地方。
- 记录早于 `model/*` 两行的、以及这一轮没有任何调用带表的，仍然是两句不同的话（都不是「没有工具」）。

真机量到的：`CALLS 0, 1, 2 SENT · 14 TOOLS`、14 行、默认 `details.open === false`；
展开第一行 → 描述 764 字符 + 定义 JSON 2145 字符（`{"type":"function","function":{"description":…`）。

## 复议（2026-09-17 深夜，牛总第三条：assistant 要显示内容 + 行会溢出）

**1. assistant 行空着——这是常态不是边缘**：一轮只发工具调用时 `content` 是 `""`，话都在 `reasoning` 里，
所以照 `text` 画的 assistant 行就是一块空标签。现在 `headline()` 三档取：有正文用正文，
没正文但有推理用 `reasoning: <第一行>`（**带前缀**，读者得知道自己在看哪一样），两样都没有写 `(no text)`。

**2. 行会溢出——根因不在行里，在列的宽度上。** 量出来：`[data-slot='trajectory-view']` 宽 **1385**，
而它只有 **1152** 可用（视口 1440 − 侧边栏），整页横向滚动、右侧那格耗时/用量被推出屏幕。
原因是 CSS 的**弹性项自动最小宽度**：`min-width: auto` 解析成内容的 min-content 宽度，而轨迹的行是
**没有空格的一整行 mono**（工具参数就是一串 JSON），于是这一列拒绝比最长的参数更窄，
`truncate` 永远轮不到生效。修法是给这条链上的弹性项逐个 `min-w-0`
（`app.tsx` 的列、视图根、列表容器）、把 `<ol>` 改成 `overflow-x-hidden`。对话页签一直没这问题，
因为它的文字是会折行的散文——**这个视图是第一份把不可断行的 mono 放进这一列的代码**。

**量到的**（同一份会话、1440 视口）：修前 view 右边缘 1673、整页横向滚动；修后 **1440**、
`scrollWidth === clientWidth === 1152`、`pageScrollsSideways: false`、每一行 `overflowsRow: false`、
右侧那格 `noteRight: 1428`（在屏幕内），长预览 `ellipsized: true`。

## 复议（2026-09-18 凌晨，牛总第四条：工具行一行到底，行上不要数字）

**工具行就是参考图那一行**：`名字 参数 → 结果`。参数与结果**各占一半、各自省略号**
（两个 `min-w-0 flex-1 truncate`），中间一个 `→`。不合成一个截断跨度的理由：那样永远是后半段先被切掉，
而后半段（结果）才是扫这一列的人在找的东西。`名字` 与 `参数` 之间**有空格**——`bash{"command":…}`
读起来像一个长标识符。

**行右侧那格时间/用量撤了**：tool 与 assistant 行的 `detailOf` 整个删掉（连同那个函数），
细节留给展开——**行上每一格都是数字，扫就变成了读**。现在四类行都是**一行 33px**。

量到的：五条行高全是 33、工具行四个 span（chip / 头 / 箭头 / 结果）、`overflow: false`、
整页不横向滚动。证据换成 `t08-trajectory-rows-one-line.png`（旧的 `t08-…-overflow` 删掉，
它记的那版行已经不在了）。

## 复议（2026-09-18 凌晨，牛总第五条：lane 的颜色要和条目一致）

**一条 lane 上的记号就是同一类条目**，所以颜色只能有一套：`input` 上的记号是用户消息、
`model` 上的是某次模型调用（也就是某条 assistant 条）、`tools` 上的是工具调用。
现在配色住在**一份表**里（`components/trajectory-colors.ts`），`KIND_HUE` 给每个 kind 两支色
（`chip` 是标签底色、`bar` 是 lane 上的那道），`LANE_KIND` 把「哪条 lane 是哪一类」写下来——
不是留给下一个加 lane 的人去猜。

之前三条 lane 全画 `bg-primary/70`。现在量出来的（浏览器 computed background）：
`input` 与 `user` 是同一个值（`oklab(0.685 -0.091 -0.142 …)`，只差 alpha 0.7 / 0.1），
`model` 与 `assistant`、`tools` 与 `tool` 同样各自成对。Tailwind 只认字面类名，所以每种 kind 的两支色
在同一行里写全——**一行表，不是两张会对不上的表**。

## 复议（2026-09-18 凌晨，同上）

面板里工具那一条的两个时长跟着 05 的改名走：`waited` 取 `resumedAt - arrivedAt`（悬置时才有），
`ran` 取 `executedAt - (resumedAt ?? arrivedAt)`。被否决的调用两格都不出现（不是 0）。

## 复议（2026-09-18 凌晨，牛总：system 的两个面要页签切换，不要上下堆）

`system` 条的详情里，提示词与工具表改成**两个页签**（`system prompt` / `tools`），默认落在提示词。
它们共用一个面板、但不再上下堆着：上下堆的结果是提示词被一大段 JSON 顶出视野；
而这两样本来就**是同一件事的两面**（提示词的 `<tools>` 块枚举的就是这些工具）。页签是**条目级**的：
只有 `system` 条有第二个面，别的条目没有这条页签栏；点开另一条 system 行会回到提示词那一页
（`ItemDetail` 按 `turn-index` 做 key，换条目即重建）。

真机验过（当前代码写出来的记录）：默认 `prompt` + 1883 字符提示词；切到 `tools` →
`CALLS 0, 1 SENT · 14 TOOLS`、14 行、首行 `anchor_grep`。**在旧记录上是空的，而这是对的**：
那些会话的 jsonl 里一条 `model/start` 都没有（`kinds` 里数得出来），所以页签里写的是
「这份记录早于那两行，说不了当时桌上有哪些工具」——不会拿今天的工具表去冒充当时的。
