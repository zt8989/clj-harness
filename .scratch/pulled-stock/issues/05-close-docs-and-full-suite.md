# 05 — 收口：文档、词表、两套全量、一次真浏览器走查

**Blocked by:** 01, 02, 03, 04

**Status:** ready-for-agent

- [ ] 词表：`CONTEXT.md` 把「存量 / 增量」「第一次拉 / 第二次拉」「投影」三组词写准；「推送缓存」这个
      词要么定义它现在就等于投影，要么删掉。
- [ ] ADR：新开一条（或按需修正 0003 决策 2/3/4、0004），把「全部存量都拉、而且两次；推只管增量；
      内存里不留增量补偿」落成裁定，并写明它取代了哪几句。
- [ ] `docs/architecture/edge.md` / `client.md`：三条门的形状、票 02 那扇区间读的门、轨迹不再是长连接、
      `events.mux` 的三类帧各承载什么，逐处按现实收口。
- [ ] `.scratch/cheap-session-load/` 的处置写清楚（并进来 / 留作前置 / 标为被取代），别留两套说法。
- [ ] 后端全量 `clojure -M:test -m harness.test-runner`；`cd ui && npm test` / `typecheck` / `build`。
- [ ] 一次真浏览器走查：打开一场长会话、点开一轮、点开一个工具、切到轨迹、断线重连，四步都在。
- [ ] 票面删除（本仓没有 `done` 这个词：做完的票从这里删掉，账留在 `spec.md` 与 git 里）。
