# 06 — 收口：文档、词表、全量、走查、票面

**What to build:** 把这一批改动落进文档与验证，不给下一个人留两个版本的真相；然后按仓库约定把票面删掉。

**Blocked by:** 01、02、03、04、05。

**Status:** ready-for-agent

- [ ] `docs/architecture/client.md`：改「状态条那五格……**它什么时候问**」那一节（四个触发与两次按需追一问
      不再存在）与「任务视图两段，各读什么 / **一个 tick**」那一节（`hooks/use-task-pane.ts` 不再是钟）；
      右栏那一节要写清现在是**按会话订阅**的帧，以及「没人看就断订阅」这条纪律换了个载体但没有变。
      侧栏那一节要写清每行的状态是**服务端推的**，以及服务端**不知道**别的进程跑的那一场。
- [ ] **「记录一律流式读」这条规矩写进它该在的地方**（`docs/architecture/edge.md` 的 jsonl 一节）：
      正面的说法 + 那份**清单**（会话 / 轨迹 / 作业记录都走流式；整份读只允许给配置文件、技能正文、
      模型点名要读的文件、HTTP 体与子进程输出）。并说清这一次清掉的是哪两处（票 01 的作业读路径、
      票 05 的 carry-back），免得下一个人再当巧合重新踩一遍。
- [ ] `docs/architecture/edge.md`：`events.mux` 那一节要说出**三类帧**（窗口 / run / 会话事实）与它们各自的
      订阅语义；`events.host` 那一节补上 listing 现在带状态；管理边路由表该补的补（`jobs` 那一行的**来源**
      换过一次，要说清它现在读的是什么）。
- [ ] **作业与记录的两层边界写进文档**（票 01 的正面）：库里那一行是**身份与状态**（id / 命令 / 起点 /
      记录路径 / 结局），记录是**内容**加一句自我完备的末行；**唯一读记录末行的地方是异常退出之后的补档**。
      顺带把「列表不再 `slurp` 每条记录」这条写进去。`job_list` 里那句 `(no command kept …)` 在新世界只对
      **别的家/更早的旧文件**成立。
- [ ] `CONTEXT.md`：「任务视图」「后台作业」「会话统计」「右栏」相关词条各补一句**它是推的**；作业那条要把
      **id 现在活过一个进程**写进去（一个词一处定义）。若「会话事实帧」需要一个词，立它，并说清它
      与窗口帧、run 帧是**同一页、同一 socket、不同类别**。
- [ ] **反转的旧决策留下日期注**，不删原文：`.scratch/right-pane-tasks/spec.md` 的决策 7（「轮询只在任务视图
      开着时跑」）与它的非目标（「不进库、不进 jsonl：作业与 runs 都是进程内的事实，刷新就空——那是老实话，
      不是这个特性的债」），以及 `docs/architecture/client.md` 里那两节。写清**当时那条理由是什么、为什么不再
      成立**：决策 7 是因为当时确实没有推的通道；「作业不进库」是被票 01 有意翻掉的（身份与状态都进库，
      记录的末行不再是读的权威）——两条的理由都写在 `spec.md` 的决策里，不重抄。
- [ ] 后端全量 `clojure -M:test -m harness.test-runner` 全绿（记下 tests / assertions / failures，以及那几行
      `ISOLATION NOTE` 到底是什么）。
- [ ] 前端全量 `cd ui && npm test`、`cd ui && npm run build` 全绿。
- [ ] **真浏览器走查**（动过 `ui/src/` 就必须）：`node scripts/dev.mjs --scripted`，自己开浏览器量六条——
      ①开了栏一整场 run，`/jobs` 与 `/api/subagents` 计数不动（关栏后也不动）；
      ②一场 run 跑完，状态条与圈是这一场的数且**零次 `/stats`**；
      ③新会话发第一句，侧栏那一行自己出现、**零次 `/api/projects`**；
      ④另一扇窗里把一场会话跑起来再 park 住，这一页侧栏那一行显示「等你回应」，而这一页一次都没问；
      ⑤**重启一次 harness**：那场会话的后台作业还在、`j1` 还是同一条命令、起点与秒数不是从零重来；
      ⑥窄窗（767px）与隐藏页面下两处都不画、也不发请求。截图进 `evidence/`。
- [ ] 落地记录写进本目录的 `spec.md`（每票建了什么、真撞上的坑是什么、实测数字），然后删掉 `issues/`。
