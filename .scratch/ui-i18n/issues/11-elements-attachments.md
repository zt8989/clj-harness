# 11 — 抄来的元素：附件与文件

**What to build:** 从用户视角：切成中文后，附件那一圈说中文——加附件的按钮与它的读屏文字、上传失败、
移除文件、`图片` / `文档` / `文件` 三个类型标签、预览面板的标题；文件行读 `未命名文件`、下载的悬停
说明；图片那一路读 `正在生成图片…`、`提供方拦下了这张图。`、`点一下放大`、`复制图片`。

**Blocked by:** 01 — 机制。本票不依赖 02（尺寸那一份已经由 02 并进那一族）。

**Status:** ready-for-agent

## 验收

- [ ] `attachment.aui`（约 9 条）、`file.tsx`（约 6 条）、`image.tsx`（约 12 条）的文案进目录
      （`elements` namespace）。
- [ ] **每一处改动逐处标 `LOCAL:`**（这三份今天一个标记都没有，都是新增）。
- [ ] 文件名、MIME、生成出来的图片正文原样穿过；`file.tsx` 里那份自己写的 `formatFileSize`
      已经由 02 并掉，本票只动它的话。
- [ ] **模型规则的拒绝句归 12**（`attachment-rules.ts` 是纯模块，与这里的展示层分开）——
      本票不动它；但要确认真机上同一件事不会一处中文一处英文。
- [ ] 真机：中文下贴一张图、展开一次附件、开一次灯箱、点一次下载（含一个没有名字的文件）。
      中英各一张截图进 `evidence/`。
- [ ] `cd ui && npm run typecheck && cd ui && npm test` 全绿。
