# 07 — 设置那扇门里的三页

**What to build:** 从用户视角：切成中文后整扇设置说中文——三个页签、General 页的每一行（含刚加的
语言那一行之外的全部）、Models 页的增删改表单与它每一句说明（含「什么都没写进去——`config.edn` 与
原来一模一样。」这类承诺句）、以及 MCP 那一页的服务器与工具两栏。

这是最大的一坨（约 76 条、两个文件），所以单独一张票。

**Blocked by:** 01, 02 — 表单里的模型数与尺寸要那一族写法。

**Status:** ready-for-agent

## 验收

- [ ] `settings-panel`（约 65 条）与 `mcp-panel`（约 11 条）、以及 `PAGES` 的三个页签标签进目录
      （`settings` namespace）。
- [ ] 面板自己那张 **keyword → 话**的闭表进目录（服务端给的是关键词，界面说的是人话）：
      没被用上的原因、MCP 服务器的状态、技能读不出来的原因。**服务端给的句子原样穿过**
      ——home 的路径、`present?` 的事实、provider 解析失败那一整句、MCP 的 error 与工具描述。
- [ ] 表单里带数字的句子走 `count`（「N 个模型 · 凭据名」那一行）。
- [ ] 真机：中文下开设置、翻三页、改一次默认档并保存、看一次「什么都没写进去」的形态、
      看 MCP 的服务器与工具两栏。中英各一张截图进 `evidence/`。
- [ ] `cd ui && npm run typecheck && cd ui && npm test` 全绿。
