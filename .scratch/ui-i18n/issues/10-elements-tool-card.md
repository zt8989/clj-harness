# 10 — 抄来的元素：工具卡

**What to build:** 从用户视角：切成中文后，展开一张工具调用卡读到的全是中文——`用过的工具`、
`已取消的工具`、取消的原因、`用户批准` / `用户拒绝`、`始终允许`、`给你的决定加一句备注`，
以及折叠头上那个 `N 次工具调用` 与思考的 `思考 (12.3s)`。

**Blocked by:** 01 — 机制。本票不依赖 02。

**Status:** ready-for-agent

## 验收

- [ ] `tool-fallback.aui`（约 18 条）、`tool-group.aui`（`N tool calls`）、`reasoning`（`Reasoning`
      与 `(N s)`）的文案进目录（`elements` namespace）。
- [ ] **每一处改动逐处标 `LOCAL:`**（这三份今天一个标记都没有，都是新增）。
- [ ] 工具名、参数、结果原样穿过（spec 决策 4）；审批选项**服务端给了 label 就用服务端的**，
      本侧那张默认标签表才进目录。
- [ ] 折叠头那个计数走 `count`（英文的 `call`/`calls`、中文的一种形式），不是 `count === 1 ? … : …`。
      **注意与 05 的分工**：05 那一行是**轮**级的摘要，这一处是**组**级的折叠头，两处各自有票。
- [ ] 真机：中文下展开三种工具卡（成功、被否决、被取消）、一次挂起中的卡、一段可折叠的思考。
      中英各一张截图进 `evidence/`。
- [ ] `cd ui && npm run typecheck && cd ui && npm test` 全绿。
