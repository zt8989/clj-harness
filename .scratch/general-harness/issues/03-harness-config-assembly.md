# 03: .harness/ 项目配置装配

**What to build:** harness.project 装配项目内 .harness/harness.edn：项目级覆盖用户级（合并语义实现后写死进 spec）；缺失 = 空档不报错，坏文件 = 指名绝对路径的异常（对齐 config.edn 缺失时的行为风格，不静默回退）。首个真实消费者 = 审批边界策略：项目声明额外免审路径或收紧全审。skills/mcp/hooks 子目录由 P1/P2 各自消费——本票只保证装配器与目录约定就位。

**Blocked by:** 02

**Status:** ready-for-agent

- [ ] 项目 .harness/harness.edn 存在且合法时被加载，项目级字段覆盖用户级同名字段
- [ ] 缺失 = 空档，工具行为与 02 收口时完全一致
- [ ] 坏文件抛指名绝对路径的异常，不静默回退
- [ ] 审批边界策略可由项目配置改变：声明的免审路径出界不 park；声明收紧时项目内也 park
- [ ] 用户级与项目级同时存在时的优先级有测试锁定
